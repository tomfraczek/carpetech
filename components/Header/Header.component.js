'use client';

import { Subheader } from '@/components/Subheader';
import { HeaderContainer } from './Header.styles';

export const Header = () => {
  return (
    <HeaderContainer>
      <Subheader />
    </HeaderContainer>
  );
};
